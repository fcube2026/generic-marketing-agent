# Force LF line endings for all text files (prevents CRLF issues in CI)
* text=auto eol=lf

# Ensure shell scripts always use LF line endings (prevents CRLF breaking
# Docker Alpine entrypoints on Windows checkouts)
*.sh text eol=lf
